on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "before build"
        run: ls
      - uses: actions/checkout@v3
      - name: "after build"
        run: ls
